apiVersion: batch/v1
kind: Job
metadata:
  name: hw1job
spec:
  backoffLimit: 1
  template:
    spec:
      containers:
      - name: hw1container
        image: multiflexer/java-app:hw1
      restartPolicy: Never
